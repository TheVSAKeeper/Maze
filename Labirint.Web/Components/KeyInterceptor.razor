<Button FullWidth
        OnClick="() => _isPause = _isPause == false"
        StartIcon="@(_isPause ? IconCatalog.Play : IconCatalog.Pause)"
        Tone="@(_isPause ? UiTone.Success : UiTone.Warning)">
    @(_isPause ? "Играть" : "Остановить")
</Button>
