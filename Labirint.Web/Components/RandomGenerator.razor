<div class="ui-row">
    <TextField @bind-Value="_seed.UserSeed"
               HelperText="@(IsGenerateRequired ? "Необходима генерация" : $"Число зерна: {_seed.CurrentSeed}")"
               Label="Зерно генерации"
               OnBlur="ResetSeed" />
    <IconButton Disabled="IsGenerateRequired"
                Icon="@IconCatalog.Share"
                OnClick="ShowShareDialogAsync"
                Title="Поделиться лабиринтом"
                Tone="UiTone.Accent" />
</div>
